# CrewAI LLM Configuration
# Used by Langchain/OpenAI integration if not overridden in code

# --- OpenAI ---
OPENAI_API_KEY="your_openai_api_key_here"
OPENAI_MODEL_NAME="gpt-4o" # Or "gpt-3.5-turbo", etc.

# --- Anthropic (Example for future extension) ---
# ANTHROPIC_API_KEY="your_anthropic_api_key_here"
# ANTHROPIC_MODEL_NAME="claude-3-opus-20240229"

# --- Tool Specific API Keys (if needed by tools) ---
# SERPER_API_KEY="your_google_serper_api_key_here" # If using SerperDevTool for WebsiteSearchTool

# --- Script Specific Defaults (Optional) ---
# DEFAULT_LLM_PROVIDER="openai" # Can be "openai", "anthropic" etc. if supported
# DEFAULT_MODEL_NAME="gpt-4o" # Overrides the provider-specific model name above if set
